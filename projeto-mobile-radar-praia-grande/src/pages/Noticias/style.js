import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      height: 600
    },
    label: {
      fontWeight: 'bold',
      margin: 10,
      marginBottom: 8
    }
});

export { styles }