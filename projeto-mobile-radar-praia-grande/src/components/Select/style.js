import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    select: {
      padding: 10,
      margin: 10,
      marginTop: 0,
      marginBottom: 6
    },
});

export { styles }