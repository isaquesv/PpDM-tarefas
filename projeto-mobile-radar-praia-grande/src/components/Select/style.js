import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    select: {
      borderWidth: 1,
      borderColor: '#aaa',
      padding: 10,
      marginHorizontal: 20,
      marginBottom: 15,
      borderRadius: 5
    },
});

export { styles }