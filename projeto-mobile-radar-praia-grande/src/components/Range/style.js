import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      marginBottom: 12,
      marginHorizontal: 20,
    },
    text: {
      textAlign: 'center',
      marginTop: 6,
    }
});

export { styles }