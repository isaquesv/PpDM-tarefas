import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    titulo: {
      fontSize: 30,
      fontStyle: 'italic',
      fontWeight: 'bold',
      textAlign: 'center',
      marginBottom: 15
    }
});

export { styles }