import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tela: {
    paddingLeft: 20,
    paddingRight: 20,
    marginBottom: 20
  },
  contador: {
    textAlign: 'center',
    fontSize: 90,
    fontWeight: 'bold'
  }
})

export { styles }