import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    imagem: {
      height: 150,
      width: 240,
      borderWidth: 1,
      margin: 'auto',
    }
});

export { styles }