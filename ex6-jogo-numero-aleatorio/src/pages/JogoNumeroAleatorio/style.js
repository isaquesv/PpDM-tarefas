import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 20
  },
  textoResultado: {
    fontSize: 32,
    fontWeight: 'bold',
    textAlign: 'center',
    color: 'green',
    paddingBottom: 5,
    marginTop: 16
  }
});

export { styles }