import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 20
  },
  textoResultado: {
    fontSize: 28,
    fontWeight: 'bold',
    textAlign: 'center',
    paddingBottom: 5,
    marginTop: 16,
    color: 'red'
  }
});

export { styles }