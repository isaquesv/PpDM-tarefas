import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    titulo: {
      fontSize: 26,
      fontStyle: 'italic',
      textAlign: 'center',
      marginBottom: 30
    }
});

export { styles }