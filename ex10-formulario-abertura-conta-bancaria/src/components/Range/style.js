import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flex:1,
      marginBottom: 12
    },
    text: {
      textAlign: 'center',
      marginTop: 6
    }
});

export { styles }