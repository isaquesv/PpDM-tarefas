import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginVertical: 20,
    marginHorizontal: 'auto'
  },
  infoText: {
    marginBottom: 12
  }
});

export { styles }