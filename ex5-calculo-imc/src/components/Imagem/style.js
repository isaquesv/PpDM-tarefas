import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    imagem: {
      height: 220,
      width: 240,
      margin: 'auto',
      marginBottom: 14
    }
});

export { styles }